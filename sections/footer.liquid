<link rel="stylesheet" href="{{ 'footer.css' | asset_url }}">

{%- liquid
  # constants
  assign breakpoint_lg = 1024
  assign img = section.settings.image
  assign m_img = section.settings.image_mobile
  if section.settings.image_mobile_ratio == 'natural'
    if m_img
      assign mobile_image_ratio = m_img.aspect_ratio
    else
      assign mobile_image_ratio = img.aspect_ratio
    endif
  else
    assign mobile_image_ratio = section.settings.image_mobile_ratio
  endif

  assign show_language = settings.show_language
  if localization.available_languages.size <= 1
    assign show_language = false
  endif

  assign show_country = settings.show_country
  if localization.available_countries.size <= 1
    assign show_country = false
  endif

  assign color_scheme = section.settings.color_scheme
%}

{%- style -%}
  @media (max-width: {{ breakpoint_lg }}px) {
    .footer__image {
      aspect-ratio: {{ mobile_image_ratio }};
    }
  }
{%- endstyle -%}

<footer class="footer bg-theme-bg text-theme-text color-scheme color-scheme--{{ color_scheme }}">
  <div class="footer__main flex">
    <div class="footer__image relative w-full lg:w-1/2">
      {%- if section.settings.link_to_home == true -%}
        <a href="{{ routes.root_url }}" aria-label="{{ shop.name }}">
      {%- endif -%}
      {%- if img != blank -%}
        {%- capture sizes -%}
          {%- render 'sizes-attribute', min: '100vw', sm: '100vw', lg: '50vw', xl: false, max: false -%}
        {%- endcapture -%}
        {%- render 'image-desktop-and-mobile',
          class: 'img-fit',
          desktop_image: img,
          mobile_image: m_img,
          src_width_mobile: 980,
          widths_mobile: '480, 980, 1200, 1400',
          src_width_desktop: 1400,
          widths_desktop: '480, 980, 1200, 1400, 1600, 1920, 2560, 3200',
          sizes: sizes
        -%}
      {%- else -%}
        {{ 'collection-4' | placeholder_svg_tag: 'media__placeholder dark img-fit' }}
        <span class="sr-only">{{ shop.name }}</span>
      {%- endif -%}
      {%- if section.settings.link_to_home == true -%}
        </a>
      {%- endif -%}
    </div>
    <div class="footer__content w-full lg:w-1/2 ">
      <div class="lg:flex lg:flex-wrap gap-x-theme">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'text' -%}
              <div class="footer-block flex flex-wrap gap-x-theme flex-col w-full footer-block--border" {{ block.shopify_attributes }}>
                {%- if block.settings.image != blank -%}
                  {% liquid
                    assign image_classes = 'footer-block__image'
                    if block.settings.enable_image_border_radius == true
                      assign image_classes = image_classes | append: ' img--border-radius-small'
                    endif
                  -%}
                  <div class="footer-block__image media relative mb-6" style="max-width: {{ block.settings.image_width }}px;">
                    {% render 'image', class: image_classes, image: block.settings.image, src_width: block.settings.image_width, srcset_2x: true %}
                  </div>
                {%- endif -%}
                <div>
                  {%- if block.settings.heading != blank  -%}
                    <h2 class="font-body large-text">{{ block.settings.heading }}</h2>
                  {%- endif -%}
                  {%- if block.settings.text != blank -%}
                    <div class="footer-block__text rte">
                      {{ block.settings.text }}
                    </div>
                  {%- endif -%}
                  {%- liquid
                    assign follow_on_shop = false
                    if shop.features.follow_on_shop? and block.settings.enable_follow_on_shop
                      assign follow_on_shop = true
                    endif
                  -%}
                  {%- if block.settings.button_label != blank or follow_on_shop == true -%}
                    <div class="footer-block__btns mt-6 flex flex-wrap gap-y-theme gap-x-theme">
                      {%- if block.settings.button_label != blank -%}
                        <a class="{{ block.settings.button_style }}" {% if block.settings.button_link == blank %}role="link" aria-disabled="true"{% else %}href="{{ block.settings.button_link }}"{% endif %}>
                          {{- block.settings.button_label | escape -}}
                        </a>
                      {%- endif -%}
                      {%- if follow_on_shop == true -%}
                        {% # theme-check-disable %}
                        {{ shop | login_button: action: 'follow' }}
                        {% # theme-check-enable %}
                      {%- endif -%}
                    </div>
                  {%- endif -%}
                </div>

                {%- if block.settings.enable_social_icons -%}
                  <div class="footer-block__social-media mt-12 w-full" style="--social-icon-color: {{ block.settings.social_icons_color }}">
                    {% render 'social-media', icon: settings.social_custom_icon, icon_height: 28, class: ' justify-start gap-y-4 gap-x-4 w-full' %}
                  </div>
                {%- endif -%}
              </div>
            {%- when 'newsletter' -%}
              <div class="footer-block footer-block--signup w-full" {{ block.shopify_attributes }}>
                {%- if block.settings.heading != blank -%}
                  <div class="footer-newsletter-title mb-6 {{ block.settings.heading_size }} {{ block.settings.heading_font }}">
                    {{ block.settings.heading }}
                  </div>
                {%- endif -%}
                {% render 'newsletter-signup', id: 'footer-signup', class: 'footer-signup' %}
                {%- if block.settings.text != blank -%}
                  <div class="footer-newsletter-text small-text mt-2">
                    {{ block.settings.text }}
                  </div>
                {%- endif -%}
              </div>

            {%- when 'link_list' -%}
              {%- if block.settings.menu != blank -%}
                <div class="footer-block footer-block--menu w-full footer-block--border" {{ block.shopify_attributes }}>
                  <footer-menu class="footer-menu flex flex-wrap justify-between gap-x-theme gap-y-theme">
                    {% for link in block.settings.menu.links %}
                      {% if link.links != blank %}
                        <details-disclosure>
                          <details class="disclosure">
                            <summary>
                              <div class="flex justify-between items-center lg:mb-2">
                                <div class="disclosure__title font-bold lg:small-text">{{ link.title | escape }}</div>
                                <span class="disclosure__toggle">
                                  {%- if settings.disclosure_toggle == 'plus' -%}
                                    {% render 'icon-plus' %}
                                  {%- else -%}
                                    {% render 'icon-chevron-down' %}
                                  {%- endif -%}
                                </span>
                              </div>
                            </summary>
                            <div class="disclosure__panel has-motion">
                              <div class="disclosure__content">
                                {%- for link in link.links -%}
                                  <div class="footer-menu__item">
                                    <a class="footer-menu__link small-text" href="{{ link.url }}">{{ link.title | escape }}</a>
                                  </div>
                                {%- endfor -%}
                              </div>
                            </div>
                          </details>
                        </details-disclosure>
                      {%- else -%}
                        <div class="summary-style disclosure__title font-bold lg:small-text">
                          <a href="{{ link.url }}" class="footer-menu__link">{{ link.title | escape }}</a>
                        </div>
                      {%- endif-%}
                    {%- endfor -%}
                  </footer-menu>
                </div>
              {%- endif -%}

          {%- endcase -%}
        {%- endfor -%}

        {%- if section.settings.enable_localization or section.settings.show_payment_icons -%}
          <div class="footer__meta footer-block w-full flex footer-block--border">
            {%- if section.settings.enable_localization -%}
              {% if show_country or show_language %}

                <button class="localization-options localization-options--footer flex gap-2 text-current js-toggle-localization" data-localization-trigger="localization-drawer-footer">
                  {% if show_language %}
                    <span class="active-language">{{ localization.language.iso_code }}</span>
                  {% endif %}
                  {% if show_country %}
                    <span class="active-currency">{{ localization.country.currency.symbol }}</span>
                  {% endif %}
                </button>
                {% render 'localization-drawer', drawer_id: 'localization-drawer-footer' %}

              {% endif %}
            {%- endif -%}
            {%- if section.settings.show_payment_icons -%}
              <div class="footer__payment">
                <span class="visually-hidden">{{ 'sections.footer.payment_methods' | t }}</span>
                <ul class="payment-icons flex flex-wrap gap-y-3 gap-x-3" role="list">
                  {%- for type in shop.enabled_payment_types -%}
                    <li>{{ type | payment_type_svg_tag: class: 'payment-icon' }}</li>
                  {%- endfor -%}
                </ul>
              </div>
            {%- endif -%}
          </div>
        {%- endif -%}

        <div class="footer-block footer-block--small">
          {%- if section.settings.secondary_menu != blank -%}
            <ul class="footer-sub-menu flex flex-wrap small-text gap-x-theme" role="list">
              {% for link in section.settings.secondary_menu.links %}
                <li class="footer-sub-menu__item">
                  <a href="{{ link.url }}">{{ link.title }}</a>
                </li>
              {% endfor %}
            </ul>
          {%- endif -%}
          {%- if section.settings.show_copyright != 'never' -%}
            <div class="footer__base footer-block footer-block--small{% if section.settings.show_copyright == 'desktop' %} footer__base--desktop-only{% endif %}">
              <span>&copy; {{ "now" | date: "%Y" }} {{ shop.name | link_to: routes.root_url }}</span>.
              {% if section.settings.show_powered_by %}
                <span>{{ powered_by_link }}</span>.
              {% endif %}
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  </div>
  {%- if section.settings.show_large_store_name == true -%}
    {%- assign logo_image = section.settings.logo_image -%}
    <div class="footer-store-name text-center font-heading mb-0">
      {%- if logo_image != blank -%}
        <div class="footer-logo-image-container">
          <div class="footer-logo-image relative w-full custom-aspect-ratio" style="--ratio: {{ logo_image.aspect_ratio }};">
            {%- capture sizes -%}
              {%- render 'sizes-attribute', min: '100vw', sm: 'calc(100vw - 80px)', xl: false, max: false -%}
            {%- endcapture -%}
            {% render 'image',
              image: logo_image,
              widths: '536, 800, 1072, 1280, 2400',
              mobile_widths: '690, 800',
              src_width: 2000,
              sizes: sizes,
              class: 'img-fit'
            %}
          </div>
        </div>
      {%- else -%}
        {% style %}
          .footer-store-name {
            font-size: {{ section.settings.large_store_name_size }}vw;
            letter-spacing: {{ section.settings.large_store_name_letter_spacing | divided_by: 1000.00 }}em;
            line-height: {{ section.settings.large_store_name_height }};
          }
        {% endstyle %}

        {%- if section.settings.large_store_name != blank -%}
          {{ section.settings.large_store_name }}
        {%- else -%}
          {{ shop.name }}
        {%- endif -%}
      {%- endif -%}
    </div>
  {%- endif -%}
</footer>

{% javascript %}
  customElements.whenDefined('details-disclosure').then(() => {
  class FooterMenu extends HTMLElement {
    constructor() {
      super();
      this.menus = this.querySelectorAll('details-disclosure');
      this.resetHandler = this.reset.bind(this);
      setTimeout(this.resetHandler, 50);
    }

    connectedCallback() {
      window.addEventListener('on:breakpoint-change', this.resetHandler);
    }

    disconnectedCallback() {
      window.removeEventListener('on:breakpoint-change', this.resetHandler);
    }

    reset() {
      this.menus.forEach((menu) => {
        if (theme.mediaMatches.lg) {
          if (!menu.disclosure.open) {
            menu.open();
          }
          menu.toggle.setAttribute('tabindex', '-1');
        } else {
          if (menu.disclosure.open) {
            menu.close();
          }
          menu.toggle.removeAttribute('tabindex');
        }
      });
    }
  }

  customElements.define('footer-menu', FooterMenu);
  });
{% endjavascript %}

{% schema %}
{
  "name": "Footer",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-2"
    },
    {
      "type": "header",
      "content": "Footer image"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image",
      "info": "Use a square image for best results."
    },
    {
      "type": "image_picker",
      "id": "image_mobile",
      "label": "Mobile image",
      "info": "Optional"
    },
    {
      "type": "select",
      "id": "image_mobile_ratio",
      "label": "Image aspect ratio for smaller screens",
      "options": [
        {
          "value": "natural",
          "label": "Natural"
        },
        {
          "value": "1.77",
          "label": "Landscape"
        },
        {
          "value": "2.77",
          "label": "Letterbox"
        },
        {
          "value": "1",
          "label": "Square"
        },
        {
          "value": "0.75",
          "label": "Portrait"
        }
      ],
      "default": "natural"
    },
    {
      "type": "checkbox",
      "id": "link_to_home",
      "label": "Link to home page",
      "default": true
    },
    {
      "type": "header",
      "content": "Large store name or logo"
    },
    {
      "type": "checkbox",
      "id": "show_large_store_name",
      "label": "Show",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "logo_image",
      "label": "Image",
      "info": "If an image is selected a text store name will not display "
    },
    {
      "type": "text",
      "id": "large_store_name",
      "label": "Replace default store name",
      "info": "Use the sliders to adjust the font size and spacing to fit the screen."
    },
    {
      "visible_if": "{{ section.settings.show_large_store_name == true and section.settings.logo_image == blank }}",
      "type": "range",
      "id": "large_store_name_size",
      "min": 5,
      "max": 30,
      "step": 0.5,
      "unit": "vw",
      "label": "Font size",
      "default": 17
    },
    {
      "visible_if": "{{ section.settings.show_large_store_name == true and section.settings.logo_image == blank }}",
      "type": "range",
      "id": "large_store_name_letter_spacing",
      "min": -100,
      "max": 300,
      "step": 10,
      "label": "Letter spacing",
      "default": 0
    },
    {
      "visible_if": "{{ section.settings.show_large_store_name == true and section.settings.logo_image == blank }}",
      "type": "range",
      "id": "large_store_name_height",
      "min": 0.5,
      "max": 2.5,
      "step": 0.1,
      "label": "Height",
      "default": 1
    },
    {
      "type": "header",
      "content": "Footer content"
    },
    {
      "type": "checkbox",
      "id": "show_payment_icons",
      "label": "Show payment icons",
      "default": true
    },
    {
      "type": "select",
      "id": "show_copyright",
      "label": "Show copyright text",
      "options": [
        {
          "value": "always",
          "label": "Always"
        },
        {
          "value": "desktop",
          "label": "Large screens only"
        },
        {
          "value": "never",
          "label": "Never"
        }
      ],
      "default": "always"
    },
    {
      "type": "checkbox",
      "id": "show_powered_by",
      "label": "Show \"Powered by Shopify\" as part of the copyright text",
      "default": true
    },
    {
      "type": "link_list",
      "id": "secondary_menu",
      "label": "Secondary menu",
      "info": "This menu won't show dropdown items."
    },
    {
      "type": "header",
      "content": "Localization"
    },
    {
      "type": "checkbox",
      "id": "enable_localization",
      "label": "Enable localization",
      "default": true
    },
    {
      "visible_if": "{{ section.settings.enable_localization }}",
      "type": "paragraph",
      "content": "Localization settings can be modified within [Theme Settings > Localization](/editor?context=theme&category=localization)"
    }
  ],
  "max_blocks": 6,
  "blocks": [
    {
      "type": "link_list",
      "name": "Footer menu",
      "settings": [
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu",
          "info": "Menus will be displayed in columns on larger screens. On mobile devices menus will be shown an accordion. Third level menu items will not be shown. [Learn more](https://cleancanvas.co.uk/support/beautify/settings/footer) ",
          "default": "footer"
        }
      ]
    },
    {
      "type": "text",
      "name": "Text, image and socials",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "range",
          "id": "image_width",
          "min": 50,
          "max": 300,
          "step": 10,
          "unit": "px",
          "label": "Maximum image width",
          "default": 150
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>Share store details, promotions, or brand content with your customers.</p>"
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label"
        },
        {
          "type": "url",
          "id": "button_link",
          "label": "Button link"
        },
        {
          "type": "select",
          "id": "button_style",
          "label": "Button style",
          "options": [
            {
              "value": "link",
              "label": "Link"
            },
            {
              "value": "btn btn--primary",
              "label": "Primary button"
            },
            {
              "value": "btn btn--secondary",
              "label": "Secondary button"
            }
          ],
          "default": "btn btn--primary"
        },
        {
          "type": "checkbox",
          "id": "enable_follow_on_shop",
          "default": true,
          "label": "Enable Follow on Shop",
          "info": "To allow customers to follow your store on the Shop app from your storefront, Shop Pay must be enabled. [Learn more](https://help.shopify.com/manual/online-store/themes/customizing-themes/follow-on-shop)"
        },
                {
          "type": "checkbox",
          "id": "enable_social_icons",
          "default": false,
          "label": "Enable Social media icons",
          "info": "Add links to your social media accounts in theme settings."
        },
        {
          "visible_if": "{{ block.settings.enable_social_icons }}",
          "type": "color",
          "id": "social_icons_color",
          "label": "Social icons color",
          "default": "#B78390"
        }
      ]
    },
    {
      "type": "newsletter",
      "name": "Email signup",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "get updates on offers and stuff you probably want to know about"
        },
        {
          "type": "select",
          "id": "heading_size",
          "label": "Heading size",
          "options": [
            {
              "value": "h4",
              "label": "Extra extra small"
            },
            {
              "value": "h3",
              "label": "Extra small"
            },
            {
              "value": "h2",
              "label": "Small"
            },
            {
              "value": "h1",
              "label": "Medium"
            },
            {
              "value": "x-large-heading",
              "label": "Large"
            },
            {
              "value": "xx-large-heading",
              "label": "Extra large"
            }
          ],
          "default": "h3"
        },
        {
          "type": "select",
          "id": "heading_font",
          "label": "Heading font",
          "options": [
            {
              "value": "font-body",
              "label": "Body font"
            },
            {
              "value": "font-heading",
              "label": "Heading font"
            }
          ],
          "default": "font-body"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Information text",
          "default": "<p>By signing up you agree to our terms</p>"
        },
        {
          "type": "paragraph",
          "content": "Customers who subscribe will have their email address added to the \"accepts marketing\" [customer list](/admin/customers?query=&accepts_marketing=1)"
        }
      ]
    }
  ]
}
{% endschema %}
